declare module 'geolocation-element-polyfill' {
	/**
	 * Polyfill for the <geolocation> HTML element.
	 * When imported, it automatically registers <geolocation> or <geo-location>
	 * as a custom element backed by the standard Geolocation API.
	 */
	const value: unknown
	export default value
}
